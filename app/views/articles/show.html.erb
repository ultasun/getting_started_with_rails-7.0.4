<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<hr />
<p><%= link_to("Edit this Article", edit_article_path(@article)) %></p>
<p>
  <%= link_to("Delete this Article", article_path(@article),
       data: {
       turbo_method: :delete,
       turbo_confirm: "Are you sure?"
      }) %>
</p>
